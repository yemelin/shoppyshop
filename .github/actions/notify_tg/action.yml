# action.yml
name: "Post Workflow Status To Telegram Channel"
description: "Post the provided message to Telegram using bot"
author: "senile-coder"

inputs:
  message:
    description: "message to be sent"
    required: true

#  TODO:
#  tg-bot-token:
#    description: "Telegram bot token used to send message to slack channel"
#    required: true
#  channel-id:
#    description: "Telegram channel where job status message will be posted"
#    required: true

runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.message }}
#    - ${{ inputs.tg-bot-token }}
#    - ${{ inputs.channel-id }}

branding:
  icon: "activity"
  color: "green"